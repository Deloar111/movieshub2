<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>M0viEsSHUB - Watch & Download</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet" />
    <style>
        * {
            font-family: 'Inter', sans-serif;
        }
        
        .font-mono {
            font-family: 'JetBrains Mono', monospace;
        }
        /* Glassmorphism effects */
        
        .glass {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .glass-dark {
            background: rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        /* Animated gradients */
        
        .gradient-bg {
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
        }
        
        @keyframes gradientShift {
            0% {
                background-position: 0% 50%;
            }
            50% {
                background-position: 100% 50%;
            }
            100% {
                background-position: 0% 50%;
            }
        }
        /* Neon glow effects */
        
        .neon-glow {
            box-shadow: 0 0 20px rgba(147, 51, 234, 0.5);
            transition: all 0.3s ease;
        }
        
        .neon-glow:hover {
            box-shadow: 0 0 30px rgba(147, 51, 234, 0.8), 0 0 60px rgba(147, 51, 234, 0.4);
        }
        /* Movie card hover effects */
        
        .movie-card {
            transform: perspective(1000px) rotateX(0deg) rotateY(0deg);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        .movie-card:hover {
            transform: perspective(1000px) rotateX(-5deg) rotateY(5deg) translateY(-10px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.6);
        }
        /* Floating animation */
        
        .float {
            animation: float 6s ease-in-out infinite;
        }
        
        @keyframes float {
            0%,
            100% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-20px);
            }
        }
        /* Pulse animation */
        
        .pulse-glow {
            animation: pulseGlow 2s ease-in-out infinite alternate;
        }
        
        @keyframes pulseGlow {
            from {
                box-shadow: 0 0 20px rgba(239, 68, 68, 0.7);
            }
            to {
                box-shadow: 0 0 40px rgba(239, 68, 68, 1), 0 0 80px rgba(239, 68, 68, 0.5);
            }
        }
        /* Scrollbar styling */
        
         ::-webkit-scrollbar {
            width: 8px;
        }
        
         ::-webkit-scrollbar-track {
            background: #1f1f1f;
        }
        
         ::-webkit-scrollbar-thumb {
            background: linear-gradient(45deg, #9333ea, #ec4899);
            border-radius: 10px;
        }
        /* Ripple effect */
        
        .ripple {
            position: relative;
            overflow: hidden;
        }
        
        .ripple::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }
        
        .ripple:hover::before {
            left: 100%;
        }
        /* Loading skeleton */
        
        .skeleton {
            background: linear-gradient(90deg, #2d2d2d 25%, #3d3d3d 50%, #2d2d2d 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }
        
        @keyframes loading {
            0% {
                background-position: 200% 0;
            }
            100% {
                background-position: -200% 0;
            }
        }
        /* Magnetic button effect */
        
        .magnetic-btn {
            transition: all 0.3s cubic-bezier(0.23, 1, 0.320, 1);
        }
        
        .magnetic-btn:hover {
            transform: translateY(-2px) scale(1.05);
        }
    </style>
</head>

<body class="bg-gradient-to-br from-gray-900 via-black to-purple-900 text-white min-h-screen">

    <!-- Animated background particles -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-20 left-10 w-2 h-2 bg-purple-500 rounded-full float opacity-60"></div>
        <div class="absolute top-40 right-20 w-1 h-1 bg-pink-500 rounded-full float opacity-40" style="animation-delay: 2s;"></div>
        <div class="absolute bottom-40 left-1/4 w-1.5 h-1.5 bg-blue-500 rounded-full float opacity-50" style="animation-delay: 4s;"></div>
        <div class="absolute bottom-20 right-1/3 w-1 h-1 bg-green-500 rounded-full float opacity-30" style="animation-delay: 1s;"></div>
    </div>

    <!-- Header with glassmorphism -->
    <header class="glass-dark sticky top-0 z-50 backdrop-blur-xl">
        <div class="max-w-7xl mx-auto px-0 py-0">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-3">
                    <div class="relative">
                        <div class="absolute inset-0 gradient-bg rounded-lg blur opacity-75"></div>
                        <h1 class="relative text-xl font-bold text-white px-3 py-1 bg-black/50 rounded-md font-mono">
                            🍿 M0viEsS HUB
                        </h1>

                    </div>
                    <div class="hidden md:flex items-center space-x-2 text-sm text-gray-400">
                        <span class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></span>
                        <span>Live</span>
                    </div>
                </div>

                <!-- Navigation pills -->
                <nav class="hidden md:flex space-x-2">
                    <a href="#" class="px-4 py-2 rounded-full glass hover:bg-white/10 transition-all duration-300">Home</a>
                    <a href="#" class="px-4 py-2 rounded-full glass hover:bg-white/10 transition-all duration-300">Movies</a>
                    <a href="#" class="px-4 py-2 rounded-full glass hover:bg-white/10 transition-all duration-300">Series</a>
                </nav>
            </div>
        </div>
    </header>

    <!-- Main Section -->
    <main class="max-w-7xl mx-auto px-4 py-8">

        <!-- Admin Add Button with magnetic effect -->
        <% if (isAdmin) { %>
            <div class="mb-8">
                <a href="/admin/add?admin=8892" class="magnetic-btn inline-flex items-center px-6 py-3 bg-gradient-to-r from-green-500 to-emerald-600 text-white rounded-xl hover:from-green-400 hover:to-emerald-500 neon-glow font-semibold space-x-2">
                    <span class="text-xl">➕</span>
                    <span>Add Movie</span>
                </a>
            </div>
            <% } %>

                <!-- Enhanced Search Bar -->
                <form method="GET" class="mb-2">
                    <div class="relative max-w-2xl mx-auto">
                        <div class="absolute inset-0 gradient-bg rounded-2xl blur opacity-30"></div>
                        <div class="relative glass rounded-2xl p-1">
                            <div class="flex">
                                <input type="text" name="search" value="<%= searchQuery %>" placeholder="Search movies, series, actors..." class="flex-1 px- py- bg-transparent text-white placeholder-gray-400 focus:outline-none text-lg" />
                                <button type="submit" class="magnetic-btn px-3 py-1.5 text-sm bg-pink-600 hover:bg-pink-500 rounded-md font-medium transition-all duration-300">
    🔍
</button>


                            </div>
                        </div>
                    </div>
                </form>

                <!-- Modern Category Pills -->
                <div class="flex flex-wrap justify-center gap-3 mb-12">
                    <% const categories = ["All Movies", "Web Series", "South Hindi Dubbed", "Bollywood", "Hollywood Hindi", "Dual Audio"]; %>
                        <% categories.forEach(cat => { %>
                            <a href="/?category=<%= cat %>" class="ripple px-3 py-1.5 text-sm rounded-full transition-all duration-300 font-medium <%= category === cat ? 'bg-gradient-to-r from-purple-500 to-pink-500 text-white neon-glow' : 'glass hover:glass-dark hover:scale-105' %>">
                                <%= cat %>
                            </a>

                            <% }) %>
                </div>

                <!-- Trending Section with enhanced design -->
                <% if (trendingMovies && trendingMovies.length > 0) { %>
                    <section class="mb-16">
                        <div class="flex items-center space-x-4 mb-8">
                            <h2 class="text-4xl font-bold bg-gradient-to-r from-red-500 to-orange-500 bg-clip-text text-transparent">
                                🔥 Trending Now
                            </h2>
                            <div class="flex-1 h-px bg-gradient-to-r from-red-500/50 to-transparent"></div>
                        </div>

                        <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-5 gap-6">
                            <% trendingMovies.forEach(movie => { %>
                                <div class="movie-card glass-dark rounded-2xl overflow-hidden group">
                                    <a href="/movies/<%= movie._id %>" class="block">
                                        <div class="relative overflow-hidden">
                                            <img src="<%= movie.poster %>" class="w-full h-64 object-cover transition-transform duration-500 group-hover:scale-110">
                                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                                            <div class="absolute top-3 right-3 bg-red-500 text-white text-xs px-2 py-1 rounded-full pulse-glow">
                                                🔥 HOT
                                            </div>
                                        </div>
                                        <div class="p-4">
                                            <h3 class="font-bold text-lg text-white group-hover:text-transparent group-hover:bg-gradient-to-r group-hover:from-purple-400 group-hover:to-pink-400 group-hover:bg-clip-text transition-all duration-300">
                                                <%= movie.title %>
                                            </h3>
                                        </div>
                                    </a>
                                </div>
                                <% }) %>
                        </div>
                    </section>
                    <% } %>

                        <!-- All Movies Grid with enhanced styling -->
                        <section class="mb-16">
                            <div class="flex items-center space-x-4 mb-8">
                                <h2 class="text-4xl font-bold bg-gradient-to-r from-blue-400 to-purple-500 bg-clip-text text-transparent">
                                    🎬 All Movies
                                </h2>
                                <div class="flex-1 h-px bg-gradient-to-r from-blue-500/50 to-transparent"></div>
                            </div>

                            <% if (movies.length === 0) { %>
                                <div class="text-center py-20">
                                    <div class="text-8xl mb-4">😢</div>
                                    <p class="text-2xl text-gray-400 mb-4">No movies found</p>
                                    <p class="text-gray-500">Try adjusting your search or filters</p>
                                </div>
                                <% } else { %>
                                    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 2xl:grid-cols-7 gap-6">
                                        <% movies.forEach(movie => { %>
                                            <div class="movie-card glass-dark rounded-2xl overflow-hidden group">
                                                <a href="/movies/<%= movie._id %>" class="block">
                                                    <div class="relative overflow-hidden">
                                                        <img src="<%= movie.poster %>" class="w-full h-64 object-cover transition-all duration-500 group-hover:scale-110 group-hover:brightness-110">
                                                        <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>

                                                        <!-- Quality badges -->
                                                        <div class="absolute top-3 left-3 flex flex-wrap gap-1">
                                                            <% movie.quality.forEach((q, index) => { %>
                                                                <span class="bg-gradient-to-r from-red-500 to-pink-500 text-white text-xs px-2 py-1 rounded-full font-medium" style="animation-delay: <%= index * 0.1 %>s;">
                                                <%= q %>
                                            </span>
                                                                <% }) %>
                                                        </div>
                                                    </div>

                                                    <div class="p-4">
                                                        <h3 class="font-bold text-base mb-2 group-hover:text-transparent group-hover:bg-gradient-to-r group-hover:from-purple-400 group-hover:to-pink-400 group-hover:bg-clip-text transition-all duration-300">
                                                            <%= movie.title %>
                                                        </h3>

                                                        <div class="flex items-center space-x-2 text-xs text-gray-400 mb-2">
                                                            <span class="w-2 h-2 bg-blue-500 rounded-full"></span>
                                                            <span><%= movie.movieLanguage %></span>
                                                        </div>
                                                    </div>
                                                </a>

                                                <!-- Admin controls with better styling -->
                                                <% if (isAdmin) { %>
                                                    <div class="px-4 pb-4 flex space-x-3">
                                                        <a href="/admin/edit/<%= movie._id %>?admin=8892" class="flex items-center space-x-1 text-yellow-400 hover:text-yellow-300 text-xs transition-colors">
                                                            <span>✏️</span>
                                                            <span>Edit</span>
                                                        </a>
                                                        <a href="/admin/delete/<%= movie._id %>?admin=8892" class="flex items-center space-x-1 text-red-400 hover:text-red-300 text-xs transition-colors">
                                                            <span>🗑️</span>
                                                            <span>Delete</span>
                                                        </a>
                                                    </div>
                                                    <% } %>
                                            </div>
                                            <% }) %>
                                    </div>
                                    <% } %>
                        </section>

                        <!-- Enhanced Pagination -->
                        <div class="flex justify-center items-center space-x-2 mb-8">
                            <% for (let i = 1; i <= totalPages; i++) { %>
                                <a href="?page=<%= i %><%= searchQuery ? '&search=' + searchQuery : '' %><%= category ? '&category=' + category : '' %>" class="magnetic-btn px-4 py-3 rounded-xl font-semibold transition-all duration-300 <%= currentPage === i ? 'bg-gradient-to-r from-red-500 to-pink-500 text-white neon-glow' : 'glass hover:glass-dark hover:scale-105' %>">
                                    <%= i %>
                                </a>
                                <% } %>
                        </div>

    </main>

    <!-- Enhanced Footer -->
    <footer class="relative mt-20">
        <div class="absolute inset-0 gradient-bg opacity-10"></div>
        <div class="relative glass-dark backdrop-blur-xl">
            <div class="max-w-7xl mx-auto px-4 py-12">
                <div class="grid md:grid-cols-3 gap-8 mb-8">
                    <div>
                        <h3 class="text-xl font-bold mb-4 bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent">M0viEsHUB</h3>
                        <p class="text-gray-400">Your ultimate destination for movies and entertainment.</p>
                    </div>
                    <div>
                        <h4 class="font-semibold mb-4 text-white">Quick Links</h4>
                        <div class="space-y-2">
                            <a href="/about-us" class="block text-gray-400 hover:text-purple-400 transition-colors">About Us</a>
                            <a href="/privacy-policy" class="block text-gray-400 hover:text-purple-400 transition-colors">Privacy Policy</a>
                            <a href="/dmca" class="block text-gray-400 hover:text-purple-400 transition-colors">DMCA</a>
                        </div>
                    </div>
                    <div>
                        <h4 class="font-semibold mb-4 text-white">Connect</h4>
                        <div class="flex space-x-4">
                            <a href="mailto:deloarhossen81722@gmail.com" class="w-10 h-10 glass rounded-full flex items-center justify-center hover:bg-white/10 transition-all duration-300">
                                📧
                            </a>
                            <a href="#" class="w-10 h-10 glass rounded-full flex items-center justify-center hover:bg-white/10 transition-all duration-300">
                                📱
                            </a>
                        </div>
                    </div>
                </div>

                <div class="border-t border-white/10 pt-8 text-center">
                    <p class="text-gray-400">
                        &copy;
                        <%= new Date().getFullYear() %> M0viEsHUB. All rights reserved.
                            <span class="text-purple-400">Made with ❤️</span>
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Add some interactive animations
        document.addEventListener('DOMContentLoaded', function() {
            // Smooth scroll for anchor links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    document.querySelector(this.getAttribute('href')).scrollIntoView({
                        behavior: 'smooth'
                    });
                });
            });

            // Add loading animation for images
            const images = document.querySelectorAll('img');
            images.forEach(img => {
                img.addEventListener('load', function() {
                    this.style.opacity = '1';
                });

                // Add skeleton loading
                if (!img.complete) {
                    img.classList.add('skeleton');
                    img.addEventListener('load', () => {
                        img.classList.remove('skeleton');
                    });
                }
            });

            // Parallax effect for background elements
            window.addEventListener('scroll', () => {
                const scrolled = window.pageYOffset;
                const rate = scrolled * -0.5;

                document.querySelectorAll('.float').forEach(el => {
                    el.style.transform = `translateY(${rate}px)`;
                });
            });
        });
    </script>

</body>

</html>