<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= movie.title %> - WatchView</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white font-sans">
    <div class="max-w-5xl mx-auto p-4">
        <a href="/" class="text-blue-400 underline">← Back to Home</a>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6 bg-gray-800 p-4 rounded-xl shadow-lg">
            <img src="<%= movie.poster %>" alt="<%= movie.title %>" class="rounded-xl w-full h-auto" />

            <div>
                <h1 class="text-3xl font-bold mb-2">
                    <%= movie.title %>
                </h1>
                <p class="text-sm text-gray-400 mb-2">
                    <%= movie.description %>
                </p>

                <p><strong>Cast:</strong>
                    <%= movie.cast.join(", ") %>
                </p>
                <p><strong>Genre:</strong>
                    <%= movie.genre.join(", ") %>
                </p>
                <p><strong>Language:</strong>
                    <%= movie.movieLanguage %>
                </p>
                <p><strong>Qualities:</strong>
                    <%= movie.quality.join(", ") %>
                </p>

                <a href="/download/<%= movie._id %>" class="mt-4 inline-block bg-blue-500 px-4 py-2 rounded hover:bg-blue-600">
                    ⬇️ Download Now
                </a>
            </div>
        </div>

        <!-- 📸 Movie Screenshots -->
        <!-- 📸 Screenshots (Compact, Responsive) -->
        <% if (movie.screenshots && movie.screenshots.length > 0) { %>
            <section class="max-w-6xl mx-auto mt-10 px-4">
                <h2 class="text-xl md:text-2xl font-bold text-pink-400 mb-4 border-b border-gray-600 pb-1 flex items-center gap-2">
                    <span>📸</span> Screenshots
                </h2>

                <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                    <% movie.screenshots.forEach((screenshot, index) => { %>
                        <div class="overflow-hidden rounded-xl border border-gray-700 shadow-md bg-black">
                            <img src="<%= screenshot %>" alt="Screenshot <%= index + 1 %>" class="w-full h-auto object-contain max-h-64" />
                        </div>
                        <% }) %>
                </div>
            </section>
            <% } %>

                <!-- Similar Movies -->
                <div class="mt-10">
                    <h2 class="text-xl font-bold text-yellow-400 mb-4">🎬 Similar Movies</h2>
                    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
                        <% suggestions.forEach(similar => { %>

                            <a href="/movies/<%= similar._id %>" class="bg-gray-800 p-2 rounded hover:shadow-lg transition">
        <img src="<%= similar.poster %>" class="w-full h-32 object-cover rounded mb-2">
        <h3 class="text-sm font-semibold"><%= similar.title %></h3>
        <p class="text-xs text-gray-400"><%= similar.movieLanguage %></p>
      </a>
                            <% }) %>
                    </div>
                </div>

    </div>
</body>

</html>