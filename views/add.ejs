<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Add Movie</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white min-h-screen p-6">
    <div class="max-w-2xl mx-auto bg-gray-800 p-6 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold mb-6 text-center text-green-400">➕ Add New Movie</h1>

        <% if (!isAdmin) { %>
            <p class="text-red-500 text-center">Access Denied. Only admins can add movies.</p>
            <% } else { %>
                <form action="/admin/add?admin=8892" method="POST" class="grid gap-4">
                    <input type="text" name="title" placeholder="Title" required class="p-3 rounded bg-gray-700" />

                    <textarea name="description" placeholder="Description" rows="4" class="p-3 rounded bg-gray-700"></textarea>

                    <input type="text" name="cast" placeholder="Cast (comma-separated)" class="p-3 rounded bg-gray-700" />
                    <input type="text" name="genre" placeholder="Genre (comma-separated)" class="p-3 rounded bg-gray-700" />
                    <input type="text" name="movieLanguage" placeholder="Language" class="p-3 rounded bg-gray-700" />
                    <input type="text" name="quality" placeholder="Quality (comma-separated)" class="p-3 rounded bg-gray-700" />

                    <input type="url" name="poster" placeholder="Poster URL" class="p-3 rounded bg-gray-700" />
                    <input type="text" name="screenshots" placeholder="Screenshot URLs (comma-separated)" class="p-3 rounded bg-gray-700" />

                    <input type="url" name="qualityLinks[480p]" placeholder="480p Link" class="p-3 rounded bg-gray-700" />
                    <input type="url" name="qualityLinks[720p]" placeholder="720p Link" class="p-3 rounded bg-gray-700" />
                    <input type="url" name="qualityLinks[1080p]" placeholder="1080p Link" class="p-3 rounded bg-gray-700" />
                    <input type="url" name="qualityLinks[other]" placeholder="Other Link" class="p-3 rounded bg-gray-700" />

                    <button type="submit" class="bg-green-600 hover:bg-green-700 px-6 py-3 rounded-lg font-semibold">
                    ➕ Add Movie
                </button>
                </form>
                <% } %>
    </div>
</body>

</html>